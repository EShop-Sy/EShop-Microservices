operations:
- type: FileShareUpload
  description: Upload files for keycloak-service
  config:
    storageAccount: ${AZURE_VOLUMES_STORAGE_ACCOUNT}
    fileShareName: ${SERVICE_KEYCLOAK_SERVICE_FILE_SHARE_BM0_NAME}
    path: ./src/Aspire/AppHost/realms
